# Что бот умеет сейчас

**Пока ничего не видно игрокам** — бот собран, но ещё ни разу не включался.

Что уже сделано:
- бот создан в Discord и добавлен на тестовый сервер `KiberPride Test`;
- продумано, как будут храниться монеты, покупки и матчи, чтобы ничего не терялось при перезапуске;
- собрана первая версия: команды `/баланс` и `/профиль`, служебный канал `kp-логи` для записей бота.

Что дальше: перезагрузить компьютер, после этого снова написать **/init-project** — бот включится
на тестовом сервере, и ты проверишь `/баланс`.

---

Обновлено: 2026-09-19.
